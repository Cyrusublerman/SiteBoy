# Host — Issues and Conflicts

## v4 turn log (2026-04-23)

- ARCH-033 (P1, FIXED) — `GenerativeToolHost` DOM creation, clearing, body attachment, and append/remove operations now route through BaseComponent helper methods.
- ARCH-001 (P2, FIXED) — `tool.md` now documents the permanent `CANVAS` sidebar tab with `SIZE` and `COLOURWAY`.
- DOC-059 (P1, FIXED) — `tool.md` sidebar contract now matches implemented host tabs: `PARAMS`, optional `ANIMATE`, and `CANVAS`; export/info are toolbar surfaces.
- DOC-060 (P2, FIXED) — `tool.md` animation/runtime description now matches host gates: `animation.type !== 'none'`, optional `sequencer`, optional `animationExport`.
