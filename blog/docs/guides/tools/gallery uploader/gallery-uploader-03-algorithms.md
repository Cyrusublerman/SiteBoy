# 03 Algorithms Library — Gallery Uploader/Manager

No algorithms library changes required. All processing (image variants, manifest patching) stays in backend tooling; UI uses ComponentLibrary only. If future algorithm use is needed, map to `assets/js/shared/algorithms/index.js` and cite sources per standards.

