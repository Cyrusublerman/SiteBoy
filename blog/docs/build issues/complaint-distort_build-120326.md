there is more. there are two separate source information bits. one in top bar and one in side bar. the one in side bar doesnt need to exist. the one in the top bar should be the width of the side bar when in landscape and 1/4 width of body when in portrait. the buttons at top are not proportional. the preview and export are not divisions of the space. the drop down for the export options is not the same width as the export button. there is an awkwards empty gap between the export button and the right border. 
in the stack the add effect button is a floating button that does not share borders which is violation. the distance between it and the descriptive text doesnt follow any logic. the descriptive text is redundant and therefore violation. the plus sign is on the left when for other dropdowns and adding things it should be on the right of the object. 
there is a double border under the button which is a clear violation. 
in the add effect dropdown everything is centered which has never been done ber. the close / filter modules divisions dont seem to be proportional. there are 2 close buttons which is idiotic. the smaller one should be removed. filter modules inst a filter it is a search so it is named wrong. the collapsable sections are styled wrong and not in line with out other collapsable elements of the site. on hover there should be popup text that gives a one sentence description of the module. 
some of the modules have "module" in their name which is redundant.
the dropdown needs its own overflow and should be on a different layer and not alter the size of the sidebar. it is missing its left right and bottom borders but has an unneccesary top border. 
the collapsable sections in the add module dropdown should be closed by default. 

the source  topbar element should say ["Source:" |  either "{Source Name}" or "Add Source +"]. it has an arrow on the right which makes no sense considering its not a drop down. 

in mobile the  sidebar has two tabs "pipeline" and "canvas' but in landscape they are not visible. 
in mobile the top buttons are almost impossible to read or use.  to save space the fit fill actual should become one button that flicks between the modes.  
what does "preview" actually do?
if I shrink the window to move into portrait mode the sidebar shits itself and nothing shows in the sections. 

if no source is loaded yet there should be text in the canvas area that is the same size as the standard text in a perfectly square box that says "upload image". you should be able to drag a file onto this or click to open. 

create a folder in @docs called "build issues" and in that a file called "distort tool build issues" 
in that describe that this page was built following the guides and that there were clear violations with the intention of the guides and thus the process must be improved. then categorise and decribe each of the issues I mentioned above, then for each find the relevent guides / standards and link them. find the relevent statements within those files and compare. find where I have had complaints but we dont have guides or standards to prevent that issue.

after that analyse all of the complaints, as well as the preexisting guides and standards and think about what trends, threads, ideologies, design logics etc are evident. are these expressed appropriately in the documentation? have a folder in build issues called "assessment {current date}" where we have separate md files for each of the topics / processes I just asked for as well as a comprehensive summary document. it needs to be as comprehensive as possible and explain all that is needed to be able to build up a strong knowledge base and guide future improvements to our whole process and system
