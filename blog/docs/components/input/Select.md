# Select

Simple select input (non-styled) alias.

## Options
- `label` (string)
- `options` (array of strings or {value,label})
- `value` (string)
- `key` (string, auto)
- Events: `onChange(value)`

## Notes
- Use `Dropdown` for styled menus.
- Full-width; uses BaseComponent helpers.

