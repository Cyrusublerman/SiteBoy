# Button

Action button.

## Options
- `text` (string)
- `size` ('s'|'m'|'l', default 'm')
- `disabled` (bool)
- `key` (string, optional)
- `onClick()` handler

## Notes
- No value; triggers handler.
- Full-width sizing via F-system.

