# Dropdown

Styled select menu.

## Options
- `label` (string)
- `options` (array of strings or {value,label})
- `value` (string | first option)
- `key` (string, auto)
- `disabled` (bool)
- Events: `onChange(value)`

## Notes
- Full-width; matches ToolBase `['dropdown', ...]` syntax.

