# ColorInput

Color picker with optional hex field.

## Options
- `label` (string)
- `value` (hex string, default '#000000')
- `showHex` (bool, default true)
- `key` (string, auto)
- Events: `onChange(value)`

## Notes
- UI colors must still be VGA/var(--c-*); tool code should enforce palette rules.

