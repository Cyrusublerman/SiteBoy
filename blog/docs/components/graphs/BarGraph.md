# BarGraph

Renders bar chart.

## Options
- `data` (array of values or {label,value})
- `colors` (optional VGA palette entries)
- `onHover`, `onSelect` (optional callbacks)

## Notes
- Uses canvas/SVG internally; respects F layout for container sizing.

