# PieGraph

Renders pie/donut chart.

## Options
- `data` (array of {label,value})
- `colors` (optional VGA palette)
- `innerRadius` (0 for pie; >0 for donut)
- `onHover`, `onSelect` (callbacks)

## Notes
- Uses F sizing via container; ensure legends/labels fit layout.

