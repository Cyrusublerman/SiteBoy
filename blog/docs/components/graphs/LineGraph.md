# LineGraph

Renders line chart.

## Options
- `data` (array of {x,y})
- `colors` (optional VGA palette)
- `onHover`, `onSelect` (callbacks)

## Notes
- Supports multiple series if provided; use F sizing for container.

