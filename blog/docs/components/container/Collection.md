# Collection

List/grid of items with uniform rendering.

## Options
- `items` (array)
- `renderItem(item, idx)` (function)
- `layout` ('list'|'grid')
- `gap` (F units)

## Notes
- Use for card lists or gallery items; renders children in chosen layout.

