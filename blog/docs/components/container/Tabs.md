# Tabs

Tabbed container.

## Options
- `tabs` (array of { id, label, content })
- `activeTab` (id, optional)
- `onChange(id)` (callback)

## Notes
- Renders tab rail + content area; uses F spacing; max 4 tabs (UI rule).

