# Grid

Grid layout container.

## Options
- `columns` (number)
- `rows` (optional; auto if omitted)
- `gap` (F units)
- `items` (array of child components/elements)

## Methods
- `render()` lays out items; add/remove via provided API in implementation.

## Notes
- Use for structured layouts; respects F spacing.

