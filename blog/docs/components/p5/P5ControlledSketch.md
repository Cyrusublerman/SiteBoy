# P5ControlledSketch

P5 sketch with external controls.

## Options
- `sketch` (fn)
- `controls` (object for parameters)
- `onUpdateControls` (callback)

## Notes
- Allows controlling sketch parameters via ToolBase UI.

