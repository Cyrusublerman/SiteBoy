# MasonryGallery

Masonry grid gallery.

## Options
- `items` (array of media items)
- `columnWidth` (optional; uses responsive defaults)
- `gap` (F units)
- `onSelect(item)` (callback)

## Notes
- Use with cloud-hosted assets and manifests; respects PCS dominance for gallery pages.

