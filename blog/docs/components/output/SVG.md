# SVG

Vector output container.

## Options
- `width`, `height` (optional; otherwise auto)
- `key` (string, optional)

## Notes
- Use for vector rendering/export; respects F-based layout via surrounding container.

