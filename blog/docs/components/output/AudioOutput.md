# AudioOutput

Web Audio output helper.

## Options
- `context` (AudioContext, optional)
- `frequency`, `waveform`, `gain` (optional initial settings)
- `key` (string, optional)

## Notes
- Use for simple audio generation/monitoring; ensure audio context is resumed on user gesture.

