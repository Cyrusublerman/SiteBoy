# Audio

Embeds audio element for playback.

## Options
- `src` (string)
- `controls` (bool, default true)
- `loop` (bool)
- `muted` (bool)
- `caption` (optional)

## Notes
- For synthesis/visualization use `AudioOutput`.

