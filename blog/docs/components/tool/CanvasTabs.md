# CanvasTabs

Canvas tab switcher for multi-view tools.

## Options
- `tabs` (array of { id, label })
- `activeTab` (id)
- `onChange(id)` (callback)
- `key` (string, optional)

## Notes
- Integrates with ToolBase canvas switching; enforces max 4 tabs (UI rule).

