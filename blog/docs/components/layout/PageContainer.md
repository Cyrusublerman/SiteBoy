# PageContainer

Root layout container for pages.

## Options
- `fluid` (bool, default true)
- `maxWidth` (optional)
- `className` (string)

## Notes
- Wraps entire page content; enforces F-system spacing.

