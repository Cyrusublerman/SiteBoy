# AnimationControls

Playback controls for animations.

## Options
- `onPlayPause`, `onStop`, `onFPSChange`, `onLoopToggle` (callbacks)
- `fps`, `loop`, `frame` (initial values)

## Notes
- Integrates with AnimationFoundation/ToolBase animation config.

