# SVGDisplay

SVG rendering helper for specialized graphics.

## Options
- `content` (SVG markup/string or builder fn)
- `width`, `height` (optional)

## Notes
- Use for static/dynamic SVG; respect F layout via container.

