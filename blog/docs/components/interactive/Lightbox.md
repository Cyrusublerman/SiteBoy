# Lightbox

Overlay viewer for media.

## Options
- `items` (array of { src, caption? })
- `startIndex` (number)
- `onClose()` (callback)

## Notes
- Provides overlay navigation; dimmed background; respects UI constraints (no extra chrome).

