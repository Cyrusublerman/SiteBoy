# ButtonGroup

Grouped buttons (segmented control).

## Options
- `items` (array of { label, value })
- `multi` (bool, default false)
- `onChange(value|values)` (callback)
- `key` (string, optional)

## Notes
- Single- or multi-select segmented buttons; F spacing.

