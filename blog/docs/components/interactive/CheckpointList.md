# CheckpointList

List of checkpoints/steps with completion state.

## Options
- `items` (array of { label, done })
- `onToggle(index, done)` (callback)

## Notes
- Use for multi-step flows; F spacing; minimal styling.

